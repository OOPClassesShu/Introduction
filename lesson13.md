[В оглавление](README.md)

# Занятие 13

# STL

**STL (Standard Template Library)** — это часть стандартной библиотеки C++, построенная на принципах обобщённого программирования. \
Она предоставляет готовые, эффективные и типобезопасные структуры данных и алгоритмы.

STL решает 
- Написание собственных коллекций (списков, векторов) для каждого проекта — долго, ошибкоопасно и неэффективно.
- Повторение типовых алгоритмов (поиск, сортировка) с разными типами данных.
- Смешение указателей, динамической памяти и ручного управления ресурсами.

Разделение данных и операций над ними. Данные хранятся в контейнерах, алгоритмы работают с ними через единый интерфейс — итераторы. \
Это позволяет одному алгоритму (например, sort) работать с вектором, списком или даже массивом.

STL строится на четырёх «китах»:

- Контейнеры — классы для хранения наборов объектов.
- Итераторы — обобщение указателей, позволяющее перебирать элементы контейнера.
- Алгоритмы — функции, выполняющие операции над последовательностями (поиск, сортировка, копирование и т.д.).
- Функторы (функциональные объекты) и адаптеры — механизмы настройки поведения алгоритмов.

Все компоненты STL — шаблонные.

Контейнеры

| Категория |	Примеры |	Особенности |
|-----------|---------|-------------|
|Последовательные|	vector, deque, list, array, forward_list |	Элементы упорядочены, порядок определяется пользователем.|
|Ассоциативные|	set, multiset, map, multimap |	Двоичное дерево поиска. Быстрый поиск O(log n). Ключи упорядочены.|
|Неупорядоченные| (хеш-таблицы, C++11)	unordered_set, unordered_map и их multi-версии |	Среднее O(1) поиск, но неупорядочены.|

## Итераторы

Итератор — объект, который «умеет»:

- передвигаться по элементам (++, --),
- получать доступ к элементу (*, ->),
- сравниваться (==, !=).

Категории итераторов (от простого к сложному):

- Input / Output — однопроходные.
- Forward — многоходовые вперёд.
- Random Access — произвольный доступ (vector, deque, array).

Пример
```
vector<int> vec = {10,20,30};
for (auto it = vec.begin(); it != vec.end(); ++it) {
    cout << *it << " ";
}
```
Диапазон [begin, end) — полуоткрытый, end указывает за последний элемент. Это универсальное соглашение STL.

## Алгоритмы

Более 100 алгоритмов в заголовке <algorithm>. Работают с итераторами, не зависят от конкретного контейнера.

```
#include <algorithm>
#include <vector>
#include <numeric>   // для accumulate

vector<int> data = {5,2,8,1,9};

// Сортировка
sort(data.begin(), data.end());          // {1,2,5,8,9}

// Поиск
auto it = find(data.begin(), data.end(), 5);
if (it != data.end()) cout << "Найден: " << *it;

// Копирование
vector<int> copy;
copy_if(data.begin(), data.end(), back_inserter(copy), [](int x){ return x > 4; });

// Сумма (из <numeric>)
int sum = accumulate(data.begin(), data.end(), 0);
```

Модифицирующие и немодифицирующие алгоритмы. Например, for_each, transform, remove_if.

## Функторы и лямбда-выражения

Алгоритмы часто позволяют передать пользовательское действие (предикат, операцию). Это можно сделать через:

- Указатель на функцию,
- Функтор (класс с перегруженным operator()),
- Лямбда-выражение.

```
sort(v.begin(), v.end(), [](int a, int b) { return a > b; }); // по убыванию

struct Greater {
    bool operator()(int a, int b) const { return a > b; }
};
sort(v.begin(), v.end(), Greater());
```

## Адаптеры контейнеров и итераторов

Адаптеры контейнеров — ограничивают интерфейс:

- stack (LIFO) — по умолчанию использует deque.
- queue (FIFO).
- priority_queue (куча).

Итераторные адаптеры:

- back_inserter, front_inserter, inserter — позволяют алгоритмам добавлять элементы в контейнер.
- reverse_iterator — обратный обход.

## std::vector

- Динамический массив, растущий по мере добавления элементов.
- Автоматическое управление памятью (конструкторы, деструктор, копирование).
- Богатый набор методов: доступ, добавление, удаление, работа с размером и ёмкостью.
- Совместимость с алгоритмами STL через итераторы.

Когда нужно хранить последовательность элементов, часто обращаться по индексу, добавлять/удалять с конца, редко — в середину. \
vector — это обёртка над динамическим массивом, которая следит за его размером и ёмкостью (capacity).

Вопросы устройства

std::vector<T> — шаблонный класс, хранящий элементы типа T в непрерывной области памяти (как обычный массив). \
Размер (size) — количество фактически хранящихся элементов. \
Ёмкость (capacity) - сколько элементов можно разместить без перевыделения памяти. \
Элементы в vector хранятся в памяти последовательно, как в массиве. Поэтому доступ по индексу O(1), а итераторы — итераторы произвольного доступа. \

Когда ёмкость превышена (например, push_back при size == capacity), vector:
- Выделяет новую область памяти бóльшего размера (обычно в 1.5–2 раза больше текущей ёмкости, зависит от реализации).
- Перемещает (или копирует) существующие элементы в новую память.
- Уничтожает старые элементы и освобождает старую память.
- Это гарантирует амортизированное O(1) для push_back — т.е. в среднем время добавления элемента не зависит от размера вектора.

Сложность основных операций

|Операция	|Сложность(время)	|Примечание|
|-----------|----------         |----------|
|Доступ по индексу ([], at)|	O(1)|	Константа
|Добавление в конец (push_back)|	Амортизированное O(1)|	Иногда O(n) при расширении|
|Удаление с конца (pop_back)	|O(1)	|Без переаллокации|
|Вставка/удаление в середине	|O(n)	|Сдвиг элементов|
|Поиск значения (линейный)	|O(n)	|Нет встроенного бинарного поиска|
|Вставка в начало	|O(n)	|Неэффективно, используйте deque|
|Изменение размера (resize)	|O(n)|	Создание/удаление элементов|
|Резервирование памяти (reserve)|	O(n)| при росте? Нет, O(0) для reserve, если new_cap > capacity, то O(n) на перемещение|

### Основные методы и примеры использования

#### Создание и инициализация

```
#include <vector>
#include <string>
using namespace std;

// Пустой вектор
vector<int> v1;

// Вектор из 5 элементов, инициализированных нулём
vector<int> v2(5);          // {0,0,0,0,0}

// Вектор из 5 элементов, инициализированных значением 42
vector<int> v3(5, 42);      // {42,42,42,42,42}

// Инициализация списком 
vector<int> v4 = {1, 2, 3, 4};

// Копирование
vector<int> v5(v4);         // копия

// Перемещение 
vector<int> v6 = std::move(v4);  // v4 теперь пуст

// Из диапазона итераторов
int arr[] = {10,20,30};
vector<int> v7(arr, arr+3);
```

#### Доступ к элементам

```
vector<int> v = {10, 20, 30, 40};

// operator[] — без проверки границ
int a = v[2];      // 30

// at() — с проверкой (бросает std::out_of_range)
int b = v.at(2);   // 30
// v.at(10) -> исключение

// front() / back()
int first = v.front(); // 10
int last = v.back();   // 40

// Получение указателя на внутренний массив (C++11)
int* ptr = v.data();   // можно использовать как массив
```

#### Размер и ёмкость

```
vector<int> v;
cout << v.size() << " " << v.capacity(); // 0 0

v.push_back(1);
cout << v.size(); // 1
cout << v.capacity(); // зависит от реализации, обычно 1 или больше

v.reserve(100);   // зарезервировать память на 100 элементов
cout << v.capacity(); // >= 100
cout << v.size(); // 1 (размер не изменился)

v.shrink_to_fit(); //запрос на уменьшение capacity до size (необязателен)
cout << v.capacity(); // может стать 1

// resize — меняет количество элементов
v.resize(5);      // добавляет 4 нуля (или default-инициализированных)
v.resize(10, 42); // добавляет 6 элементов со значением 42
v.resize(2);      // удаляет лишние элементы с конца
```

#### Добавление и удаление элементов

```
vector<int> v = {1, 2, 3};

// Добавление в конец
v.push_back(4);          // {1,2,3,4}

// Добавление в конец с конструированием на месте (C++11)
v.emplace_back(5);       // {1,2,3,4,5} (эффективнее push_back для сложных типов)

// Вставка в середину
v.insert(v.begin() + 2, 99);   // {1,2,99,3,4,5}
v.insert(v.begin(), 100);      // в начало: {100,1,2,99,...}

// Вставка нескольких копий
v.insert(v.end(), 3, 88);      // три 88 в конец

// Вставка из списка или диапазона
v.insert(v.begin(), {10,20});

// Удаление с конца
v.pop_back();   // удаляет последний

// Удаление по позиции (или диапазону)
v.erase(v.begin() + 2);        // удаляет элемент по индексу 2
v.erase(v.begin(), v.begin()+3); // удаляет первые 3 элемента

// Очистка всех элементов
v.clear();      // size = 0, capacity обычно не меняется
```

#### Итераторы и циклы

```
vector<int> v = {1,2,3,4};

// Через индексы
for (size_t i = 0; i < v.size(); ++i) cout << v[i] << " ";

// Через итераторы (классический)
for (auto it = v.begin(); it != v.end(); ++it) cout << *it << " ";

// Диапазонный for (C++11)
for (int x : v) cout << x << " ";

// Обратный итератор
for (auto rit = v.rbegin(); rit != v.rend(); ++rit) cout << *rit << " ";
```

#### Операции сравнения и присваивания

```
vector<int> a = {1,2,3};
vector<int> b = {1,2,3};
vector<int> c = {1,2,4};

a == b; // true
a < c;  // true (лексикографическое сравнение)

// Присваивание
a = c;  // копирование
a = {5,6,7}; // присваивание списка
a.assign(5, 100); // 5 элементов 100
```

#### Особенности работы с пользовательскими типами
```
struct Person {
    string name;
    int age;
    Person(const string& n, int a) : name(n), age(a) {}
    // Если не определить конструктор копирования/перемещения, они будут сгенерированы
};

vector<Person> people;
people.emplace_back("Alice", 30); // конструирование на месте
people.push_back(Person("Bob", 25)); // временный объект + копирование/перемещение

// Если тип некопируемый (например, unique_ptr), можно использовать только перемещение
vector<unique_ptr<int>> ptrs;
ptrs.push_back(make_unique<int>(5)); // ОК, перемещение
```

При перевыделении памяти элементы должны быть перемещаемыми или копируемыми. \
Если тип noexcept перемещаемый, вектор использует перемещение для эффективности; иначе — копирование.

####  Инвалидация итераторов

Итераторы, ссылки и указатели на элементы могут стать недействительными после некоторых операций:

|Операция	|Инвалидация|
|-----------|-----------|
|reserve / resize| (при росте ёмкости)	Все итераторы, ссылки, указатели (переаллокация)|
|push_back / emplace_back| при size == capacity|	Все (переаллокация)|
|push_back / emplace_back без переаллокации	|Нет|
|insert / erase|	Итераторы после позиции вставки/удаления|
|pop_back|	Итератор на удалённый элемент (end() может стать другим)|
|clear|	Все итераторы (кроме end)|

Пример ошибки:
```
vector<int> v = {1,2,3};
auto it = v.begin();
v.push_back(4);   // может вызвать переаллокацию, it становится невалидным
cout << *it;      // НЕОПРЕДЕЛЁННОЕ ПОВЕДЕНИЕ!
```

#### Резервирование для ускорения

Если вы заранее знаете, сколько элементов будет добавлено, вызовите reserve:
```
vector<int> v;
v.reserve(1000);   // одна переаллокация
for (int i = 0; i < 1000; ++i) v.push_back(i);
```

#### Обмен содержимого
swap двух векторов обменивает их внутренние указатели (константное время) и не вызывает копирования элементов. \
Полезно для освобождения памяти:

#### Задачки
```
vector<int> v(1000); // занимает память
v.clear();           // size=0, но capacity обычно остаётся
vector<int>().swap(v); // обмен с пустым временным вектором -> v.capacity() = 0
```

```
Реализовать программу, которая читает строки из файла, сохраняет их в vector,
удаляет пустые строки, сортирует оставшиеся по длине (от самых коротких к длинным)
и выводит первые 10 уникальных строк (без учёта регистра). Использовать алгоритмы STL.
```

```
Дан вектор целых чисел. Разделите его на два вектора: evens (чётные) и odds (нечётные).
Исходный вектор не изменять.
```

```
Дан вектор целых чисел. Вычислите среднее арифметическое (как double). Затем создайте новый вектор,
содержащий только те элементы, которые больше среднего.```
```

```
Дан вектор символов (или строк). Проверьте, является ли он палиндромом
(читается одинаково слева направо и справа налево).
```

```
Реализуйте функцию void reverseVector(std::vector<int>& v), которая разворачивает вектор (первый становится последним и т.д.)
за линейное время, используя только std::swap (или обмен через временную переменную).
Не используйте std::reverse.
```

## std::list — двусвязный список

std::list<T> — шаблонный класс, реализующий двусвязный список. Каждый узел содержит:

- значение типа T,
- указатель на следующий узел,
- указатель на предыдущий узел.

Свойства:

- Элементы не хранятся в непрерывной памяти (в отличие от vector).
- Итераторы — двунаправленные (bidirectional), поддерживают ++ и --, но не +n или [].
- Вставка и удаление не инвалидируют другие итераторы (кроме итератора на удаляемый элемент).
- Список не вызывает переаллокации всей памяти при росте — новые узлы выделяются по одному.

Сложность основных операций

|Операция	|Сложность|
|-----------|---------|
|Доступ к первому/последнему элементу|	O(1)|
|Доступ к произвольному элементу|	O(n)|
|Вставка/удаление в начале/конце|	O(1)|
|Вставка/удаление в середине (если известна позиция)|	O(1)|
|Поиск элемента по значению|	O(n)|
|Получение размера (size())|	O(1) |
|Слияние двух списков (splice)|	O(1)|

### Основные методы и примеры использования

#### Создание и инициализация

```
#include <list>
#include <string>
using namespace std;

list<int> l1;                       // пустой
list<int> l2(5, 100);               // 5 элементов со значением 100
list<int> l3 = {1,2,3,4};           // инициализация списком
list<int> l4(l3);                   // копирование
list<int> l5(move(l3));             // перемещение (l3 становится пустым)
```

#### Доступ к элементам

```
list<int> l = {10,20,30};
int first = l.front();   // 10
int last  = l.back();    // 30
// l[1] - НЕЛЬЗЯ, operator[] отсутствует
```
#### Добавление и удаление элементов

```
list<int> l = {1,2,3};

l.push_back(4);          // {1,2,3,4}
l.push_front(0);         // {0,1,2,3,4}

l.pop_back();            // {0,1,2,3}
l.pop_front();           // {1,2,3}

// Вставка по итератору
auto it = l.begin();
++it;                    // указывает на 2
l.insert(it, 99);        // {1,99,2,3}

// Вставка нескольких копий
l.insert(it, 2, 77);     // {1,99,77,77,2,3}

// Удаление по итератору
it = l.begin();
l.erase(it);             // удалили 1 -> {99,77,77,2,3}

// Удаление диапазона
l.erase(l.begin(), next(l.begin(), 2)); // удалили 99,77 -> {77,2,3}
```

#### Специфические методы list

```
list<int> l1 = {1,2,3};
list<int> l2 = {4,5,6};

// Перемещение (splice) — перекладывает узлы из одного списка в другой
auto it = l1.begin();
advance(it, 2);          // указывает на 3
l1.splice(it, l2);       // вставить все элементы l2 перед 3, l2 становится пустым
// l1: {1,2,4,5,6,3}

// Сортировка (собственный метод, эффективнее, чем общий sort для списков)
l1.sort();               // по возрастанию
l1.sort(greater<int>()); // по убыванию

// Удаление дубликатов (только для отсортированного списка)
l1.unique();             // удаляет последовательные дубликаты

// Удаление элементов по условию
l1.remove(5);            // удалить все элементы == 5
l1.remove_if([](int n) { return n % 2 == 0; }); // удалить чётные

// Слияние двух отсортированных списков
l1.merge(l2);            // l1 и l2 должны быть отсортированы, результат в l1, l2 пуст
```

#### Итераторы и циклы

```
list<int> l = {1,2,3,4,5};

// Диапазонный for
for (int x : l) cout << x << " ";

// Ручные итераторы
for (auto it = l.begin(); it != l.end(); ++it) cout << *it << " ";

// Обратный обход
for (auto rit = l.rbegin(); rit != l.rend(); ++rit) cout << *rit << " ";
```

#### Размер и ёмкость

```
list<int> l = {1,2,3,4,5};
auto it = l.begin();   // указывает на 1
auto it2 = next(it);   // указывает на 2
l.erase(it);           // it инвалидирован, но it2 остаётся валидным (указывает на 2)
cout << *it2;          // OK, выведет 2
```

#### Инвалидация итераторов

Вставка и удаление не инвалидируют другие итераторы, кроме тех, что указывают на удаляемый элемент.

```
list<int> l = {1,2,3,4,5};
auto it = l.begin();   // указывает на 1
auto it2 = next(it);   // указывает на 2
l.erase(it);           // it инвалидирован, но it2 остаётся валидным (указывает на 2)
cout << *it2;          // OK, выведет 2
```

### Доп информация

Класс list содержит следующие функции-члены:
```
template <class InIter> void assign(InIter start, InIter end); Помещает в список последовательность, определяемую параметрами start и end.
```
```
void assign(size_type num, const T &val); Помещает в список num элементов со значением val.
```
```
reference back(); const_reference back() const; Возвращает ссылку на последний элемент в списке.
```
```
iterator begin(); const_iterator begin() const; Возвращает итератор для первого элемента в списке.
```
```
void clear(); Удаляет все элементы из списка.
```
```
bool empty() const; Возвращает значение истины, если используемый список пуст, и значение лжи в противном случае.
```
```
const_iterator end() const; iterator end(); Возвращает итератор для конца списка.
```
```
iterator erase(iterator i); Удаляет элемент, адресуемый итератором i, возвращает итератор для элемента, расположенного после удаленного.
```
```
iterator erase(iterator start, iterator end); Удаляет элементы в диапазоне, задаваемом параметрами start и end, возвращает итератор для элемента,
расположенного за последним удалённым элементом.
```
```
reference front(); const_reference front() const; Возвращает ссылку на первый элемент в списке.
```
```
allocator_type get_allocator() const; Возвращает распределитель списка.
```
```
iterator insert(iterator i, const T &val = T()); Вставляет значение val непосредственно перед элементом, заданным параметром i, возвращает итератор для этого элемента.
```
```
void insert(iterator i, size_type num, const T &val); Вставляет num копий значения val непосредственно перед элементом, заданным параметром i.
```
```
template <class InIter> void insert(iterator i,  InIter start, InIter end);Вставляет в список последовательность, определяемую параметрами start и end,
непосредственно перед элементом, заданным параметром i.
```
```
size_type max_size() const; Возвращает максимальное число элементов, которое может содержать список.
```
```
void merge(list<T,Allocator> &ob);
template <class Comp> void merge(list<T,Allocator>  &ob, Comp cmpfn);
Объединяет упорядоченный список, содержащийся в объекте ob, с данным упорядоченным списком.
Результат также упорядочивается. После объединения список, содержащийся в объекте ob, остается пустым.
Во второй форме может быть задана функция сравнения, которая определяет, когда один элемент меньше другого.
```
```
void pop_back(); Удаляет последний элемент в списке.
```
```
void pop_front(); Удаляет первый элемент в списке.
```
```
void push_back(const T &val); Добавляет в конец списка элемент со значением, заданным параметром val.
```
```
void push_front(const T &val); Добавляет в начало списка элемент со значением, заданным параметром val.
```
```
reverse_iterator rbegin(); const_reverse_iterator rbegin() const; Возвращает реверсивный итератор для конца списка.
```
```
reverse_iterator rend(); const_reverse_iterator rbegin() const; Возвращает реверсивный итератор для начала списка.
```
```
void remove(const T &val); Удаляет из списка элементы со значением, заданным параметром val.
```
```
template <class UnPred> void remove_if(UnPred pr); Удаляет элементы, для которых унарный предикат pr равен значению true.
```
```
void resize(size_type num, const T &val = T()); Устанавливает емкость списка равной не менее заданного значения num,
если вектор для этого нужно удлинить, то в его конец добавляются элементы о значением, заданным параметром val.
```
```
void reverse(); Реверсирует список.
```
```
size_type size() const; Возвращает текущее количество элементов в списке.
```
```
void sort();
template <class Comp> void sort(Comp cmpfn);
Сортирует список. Вторая форма сортирует список с помощью функции сравнения cmpfn, чтобы определять, когда один элемент меньше другого.
```
```
void splice(iterator i, list<T,Allocator> &ob);
Вставляет содержимое списка ob в данный список в позиции, указанной итератором i. После выполнения этой операции список ob остается пустым.
```
```
void splice(iterator i, list<T,Allocator> &ob, iterator el);
Удаляет из списка ob элемент, адресуемый итератором el, и сохраняет его в позиции, адресуемой итератором i.
```
```
void splice(iterator i, list<T,Allocator> &ob, iterator start, iterator end);
Удаляет из списка ob диапазон, определяемый параметрами start и end, и сохраняет его в данном списке,
начиная с позиции, адресуемой итератором i.
```
```
void swap(list<T,Allocator> &ob); Выполняет обмен элементами данного списка и списка ob.
```
```
void unique(); template <class BinPred> void unique(BinPred pr); Удаляет из списка элементы-дубликаты. Вторая форма для определения уникальности использует предикат pr.
```

## std::forward_list — односвязный список

Отличия от std::list

|Характеристика	|std::list (двусвязный)	|std::forward_list (односвязный)
|---------------|-----------------------|------------------------------|
|Направление обхода|	Вперёд и назад	|Только вперёд|
|Итераторы	|Bidirectional|	Forward|
|Память на узел|	Два указателя (prev/next)|	Один указатель (next)|
|push_back, pop_back, back(), size()|	Есть|	Нет|
|insert, erase|	Есть (с итератором)|	insert_after, erase_after|
|reverse|	Есть|	Есть|
|splice|	Есть несколько версий|	splice_after|

### Внутреннее устройство
Узел содержит только data и указатель на следующий узел. Головной элемент не имеет предыдущего. \
Итераторы — только forward, поддерживают ++, но не --.

#### Основные методы

```
#include <forward_list>
using namespace std;

forward_list<int> fl = {1,2,3,4};

// Доступ к первому элементу
int first = fl.front();   // 1
// back() отсутствует

// Добавление/удаление в начале
fl.push_front(0);         // {0,1,2,3,4}
fl.pop_front();           // {1,2,3,4}

// Вставка после итератора
auto it = fl.begin();      // указывает на 1
fl.insert_after(it, 99);   // вставить 99 после 1 -> {1,99,2,3,4}

// Вставка нескольких
fl.insert_after(it, 2, 77); // {1,77,77,99,2,3,4} (вставляет после 1, но обратите внимание: it не изменился)

// Удаление после итератора
fl.erase_after(it);        // удаляет элемент после 1 -> {1,77,99,2,3,4} (первый 77 удалён)

// Удаление диапазона после итератора
fl.erase_after(it, fl.end()); // удаляет всё после 1 -> {1}

// Специальные методы (аналоги list, но с суффиксом _after)
fl.sort();
fl.unique();
fl.remove(77);
fl.remove_if([](int n){ return n>3; });

// Слияние (оба должны быть отсортированы)
forward_list<int> fl2 = {5,6};
fl.merge(fl2);   // fl2 становится пустым

// Перемещение (splice_after)
forward_list<int> src = {10,20};
auto pos = fl.before_begin(); // итератор перед первым элементом
fl.splice_after(pos, src);    // вставить src после pos (т.е. в начало)
```
```
Задача 1.
Дан std::list<int>. Переместите все чётные числа в начало списка, сохранив их относительный порядок, а нечётные — в конец,
также сохранив порядок.

(Подсказка: используйте splice.)

Задача 2.
Реализуйте функцию void removeEveryOther(std::forward_list<int>& fl), которая удаляет каждый второй элемент (начиная со второго).
Пример: {1,2,3,4,5} → {1,3,5}.

Задача 3. (ДЗ)
Даны два отсортированных std::list<int>. Объедините их в один отсортированный список, не используя merge (напишите свой алгоритм слияния).
```


## std::set — множество уникальных элементов

std::set<T> — шаблонный класс, хранящий уникальные значения типа T в отсортированном порядке.

Свойства:

- Все элементы уникальны (попытка вставить существующий элемент игнорируется).
- Элементы неизменяемы после вставки (изменение значения нарушило бы упорядоченность).
- Итераторы — двунаправленные (bidirectional), обход выполняется в порядке возрастания ключа.
- Операции поиска, вставки и удаления имеют сложность O(log n).

Сложность основных операций

|Операция|	Сложность|
|--------|-----------|
|Поиск элемента (find)|	O(log n)|
|Вставка (insert)	|O(log n)|
|Удаление (erase по итератору)	| O(1) (с учётом балансировки)|
|Удаление (erase по ключу)	|O(log n)|
|Доступ к минимальному/максимальному|	O(1) (через begin()/rbegin())|
|Проверка наличия (count)	|O(log n)|
|Размер (size)	|O(1)|

### Создание и инициализация

```
#include <set>
#include <string>
#include <iostream>
using namespace std;

set<int> s1;                     // пустое множество
set<int> s2 = {3,1,4,1,5,9};    // дубликат 1 будет проигнорирован -> {1,3,4,5,9}
set<int> s3(s2);                 // копирование
set<int> s4(move(s3));           // перемещение (s3 становится пустым)

// Свой порядок сортировки (по убыванию)
set<int, greater<int>> s5 = {1,2,3}; // {3,2,1}

// Сортировка по пользовательскому критерию
struct CompareByLength {
    bool operator()(const string& a, const string& b) const {
        if (a.size() != b.size()) return a.size() < b.size();
        return a < b; // лексикографически при равной длине
    }
};
set<string, CompareByLength> s6 = {"apple", "banana", "kiwi", "pear"};
// порядок: "kiwi"(4), "pear"(4), "apple"(5), "banana"(6)
```

### Добавление и удаление элементов

```
set<int> s = {1,2,3};

// Вставка
auto result = s.insert(4);   // result.first - итератор на 4, result.second = true
s.insert(2);                 // игнорируется, 2 уже есть
s.insert({5,6,7});           // вставка нескольких

// Вставка с подсказкой (оптимизация)
auto it = s.find(3);
s.insert(it, 8);             // если подсказка правильная, вставка быстрее

// Удаление
s.erase(3);                  // удаление по ключу, возвращает количество удалённых (0 или 1)
s.erase(s.begin());          // удаление по итератору
s.erase(s.begin(), s.end()); // удаление диапазона (аналог clear)
s.clear();                   // очистка
```

### Поиск и проверка

```
set<int> s = {10,20,30};

// find - возвращает итератор или end()
auto it = s.find(20);
if (it != s.end()) cout << "Найден: " << *it << endl;

// count - проверка наличия (для set возвращает 0 или 1)
if (s.count(40)) cout << "40 есть";

// lower_bound / upper_bound / equal_range
auto low = s.lower_bound(15); // первый элемент >= 15 -> 20
auto up  = s.upper_bound(25); // первый элемент > 25 -> 30
auto range = s.equal_range(20); // pair(lower_bound, upper_bound) для 20
```

### Итераторы и обход

```
set<int> s = {5,2,8,1,9};

// Обход в порядке возрастания (по умолчанию)
for (int x : s) cout << x << " ";   // 1 2 5 8 9

// Через итераторы
for (auto it = s.begin(); it != s.end(); ++it) cout << *it << " ";

// Обратный обход (убывание)
for (auto rit = s.rbegin(); rit != s.rend(); ++rit) cout << *rit << " "; // 9 8 5 2 1
```

## std::map — ассоциативный массив (словарь)

Свойства:
- Ключи уникальны (один ключ — одно значение).
- Доступ по ключу за O(log n).
- Оператор [] позволяет удобно добавлять/изменять элементы.

Сложность основных операций

|Операция|	Сложность|
|--------|-----------|
|Доступ по ключу (at, [])	|O(log n)|
|Поиск (find)	|O(log n)|
|Вставка (insert)|	O(log n)|
|Удаление (erase)|	O(log n) |
|count	O(log n)|
|lower_bound/upper_bound|	O(log n)|

### Создание и инициализация

```
#include <map>
#include <string>

map<int, string> m1;                      // пустая
map<int, string> m2 = {{1, "one"}, {2, "two"}, {3, "three"}};

// С пользовательским компаратором (по убыванию ключей)
map<int, string, greater<int>> m3 = {{1, "a"}, {2, "b"}}; // {2:"b", 1:"a"}

// Копирование и перемещение
map<int, string> m4(m2);
map<int, string> m5(move(m2));
```

### Вставка элементов

```
map<int, string> m;

// Способ 1: operator[]
m[1] = "one";    // если ключа нет — создаётся пара (1, ""), затем присваивается "one"
m[2] = "two";

// Способ 2: insert с парой
m.insert({3, "three"});
m.insert(make_pair(4, "four"));
m.insert(pair<int,string>(5, "five"));

// Способ 3: emplace (конструирует на месте)
m.emplace(6, "six");

// Способ 4: insert с проверкой
auto result = m.insert({2, "TWO"}); // result.second == false, т.к. ключ 2 уже есть
if (!result.second) {
    cout << "Ключ 2 уже существует, значение: " << result.first->second << endl;
}

// Вставка с подсказкой
auto it = m.find(3);
m.insert(it, {7, "seven"});
```

### Доступ к элементам

```
map<int, string> m = {{1, "one"}, {2, "two"}, {3, "three"}};

// operator[] — если ключа нет, создаёт пару с default-инициализированным значением
string s = m[2];        // "two"
string t = m[10];       // создаётся пара (10, "") и возвращается ссылка на пустую строку

// at() — выбрасывает std::out_of_range, если ключа нет
string u = m.at(1);     // "one"
// m.at(100);           // исключение

// find — возвращает итератор или end()
auto it = m.find(2);
if (it != m.end()) cout << it->first << " -> " << it->second;

// Доступ к первому/последнему (по ключу)
auto first = *m.begin();        // пара (1,"one")
auto last  = *m.rbegin();       // пара (3,"three")
```

### Удаление элементов

```
map<int, string> m = {{1,"a"},{2,"b"},{3,"c"},{4,"d"}};

// По ключу
m.erase(2);                // удаляет пару с ключом 2

// По итератору
auto it = m.find(3);
if (it != m.end()) m.erase(it);

// По диапазону
m.erase(m.begin(), m.find(4)); // удаляет {1,"a"} (если ключи 1,2,3,4)

// Очистка
m.clear();
```

### Обход и итераторы

```
map<int, string> m = {{2,"two"}, {1,"one"}, {3,"three"}};

// Диапазонный for (элементы отсортированы по ключу)
for (const auto& [key, value] : m) {   // C++17 structured binding
    cout << key << " -> " << value << endl;
}
// Вывод: 1 -> one, 2 -> two, 3 -> three

// Через итераторы
for (auto it = m.begin(); it != m.end(); ++it) {
    cout << it->first << " => " << it->second << endl;
}

// Обратный порядок
for (auto rit = m.rbegin(); rit != m.rend(); ++rit) {
    cout << rit->first << " => " << rit->second << endl;
}
```

### Дополнительно
```
iterator begin();
const_iterator begin() const;
Возвращает итератор для первого элемента в отображении.
```
```
void clear();
Удаляет все элементы из отображения.
```
```
size_type count(const key_type &k) const;
Возвращает число вхождений ключа k в отображении (1 или 0).
```
```
const_iterator end() const;
iterator end();
Возвращает итератор, указывающий на конец отображения.
```
```
pair<iterator, iterator> equal_range(const key_type &k);
pair<const_iterator, const_iterator> equal_range(const key_type &k) const;
Возвращает пару итераторов, которые указывают на первый и последний элементы в отображении, содержащие заданный ключ.
```
```
void erase(iterator i);
Удаляет элемент, адресуемый итератором i.
```
```
void erase(iterator start, iterator end);
Удаляет элементы в диапазоне, задаваемом параметрами start и end.
```
```
size_type erase(const key_type &k);
Удаляет из отображения элементы, ключи которых имеют значение k.
```
```
iterator find(const key_type &k);
const_iterator find(const key_type &k) const;
Возвращает итератор для заданного ключа. Если ключ не обнаружен, возвращает итератор до конца отображения.
```
```
allocator_type get_allocator() const;
Возвращает распределитель отображения.
```
```
iterator insert(iterator i, const value_type &val);
Вставляет значение val после элемента, заданным итератором i, возвращает итератор для этого элемента.
```
```
template <class InIter> void insert(InIter start, InIter end);
Вставляет элементы заданного диапазона.
```
```
pair<iterator, bool> insert(const value_type &val);
Вставляет значение val в используемое отображение.
Возвращает итератор для данного отображения. Элемент вставляет только в том случае, если его еще нет в отображении.
Если элемент был вставлен возвращает пару pair<iterator, true>, в противном случае pair<iterator, false>.
```
```
key_compare key_comp() const;
Возвращает объект-функцию, которая сравнивает ключи.
```
```
iterator lower_bound(const key_type &k);
const_iterator lower_bound(const key_type &k) const;
Возвращает итератор для первого элемента в отображении, ключ которого равен значению k или больше
этого значения.
```
```
size_type max_size() const;
Возвращает максимальное число элементов, которое может содержать отображение.
```
```
reference operator[](const key_type &i);
Возвращает ссылку на элемент, заданный параметром i. Если этого элемента не существует, вставляет его в отображение.
```
```
reverse_iterator rbegin();
const_reverse_iterator rbegin() const;
Возвращает реверсивный итератор для конца отображения.
```
```
reverse_iterator rend();
const_reverse_iterator rend() const;
Возвращает реверсивный итератор для начала отображения.
```
```
size_type size() const;
Возвращает текущее количество элементов в отображении.
```
```
void swap(map<Key, T, Comp, Allocator> &ob);
Выполняет обмен элементами данного отображения и отображения ob.
```
```
iterator upper_bound(const key_type &k);
const_iterator upper_bound(const key_type &k) const;
Возвращает итератор для первого элемента в отображении, ключ которого больше заданного значения k.
```
```
value_compare value_comp() const;
Возвращает объект-функцию, которая сравнивает значения.
```


Задача 1. 
```
Дан массив целых чисел. Выведите все уникальные элементы в порядке возрастания, а также их количество.
```
Задача 2. 
```
Реализуйте функцию, которая принимает два set<int> и возвращает их объединение, пересечение и разность.
```
Задача 3. 
```
Дан текст. Подсчитайте, сколько раз встречается каждое слово, и выведите слова в порядке убывания частоты
(при одинаковой частоте — в алфавитном порядке).
(Подсказка: используйте map для подсчёта, затем set или vector с сортировкой.)
```
Задача 4. 
```
Реализуйте простой кэш с истечением времени: TimedCache<K, V>, где элементы автоматически удаляются через заданный интервал.
Используйте map для хранения пар (ключ, значение) и отдельный multimap для хранения времени истечения.
```
Задача 5. 
```
Дан map<string, int> с оценками студентов. Удалите всех студентов с оценкой ниже 60. Выведите оставшихся.
```

Задача 6. 
```
С помощью multiset найдите медиану потока чисел (поступающих одно за другим) за O(log n) на добавление.
```


## std::multiset множество с дубликатами

Отличия от std::set


|Характеристика	|std::set<T>|	std::multiset<T>|
|--------------|------------|-------------------|
|Уникальность элементов|	Да (каждый элемент уникален)|	Нет (допускаются дубликаты)|
|Вставка элемента|	insert возвращает pair<iterator,bool>|	insert возвращает iterator (всегда успешна)|
|Удаление по ключу (erase(key))|	Удаляет 0 или 1 элемент|	Удаляет все элементы с данным ключом|
|count(key)|	Возвращает 0 или 1|	Возвращает количество дубликатов|
|find(key)|	Возвращает итератор на элемент|	Возвращает итератор на первый из дубликатов (произвольный)|
|lower_bound/upper_bound|	Работают как обычно, диапазон из 0–1 элемента|	Диапазон может содержать много элементов|


### Основные методы и примеры
```
#include <set>
#include <iostream>

int main() {
    std::multiset<int> ms = {1, 2, 2, 3, 3, 3};

    // Вставка
    ms.insert(2);      // теперь {1,2,2,2,3,3,3}
    auto it = ms.insert(4);  // it указывает на 4

    // Подсчёт дубликатов
    std::cout << "count of 2: " << ms.count(2) << std::endl; // 3

    // Поиск первого из дубликатов
    auto found = ms.find(3);
    std::cout << "first 3: " << *found << std::endl;

    // Диапазон всех 3
    auto range = ms.equal_range(3); // pair(нижняя_граница, верхняя_граница)
    for (auto it = range.first; it != range.second; ++it) {
        std::cout << *it << " ";    // 3 3 3
    }
    std::cout << std::endl;

    // Удаление одного элемента (по итератору)
    auto to_delete = ms.find(2);
    ms.erase(to_delete); // удаляет только один элемент 2

    // Удаление всех 2
    ms.erase(2);         // удаляет все оставшиеся двойки

    // Вывод всех элементов
    for (int x : ms) std::cout << x << " "; // 1 3 3 3 4
}
```

### Специфические методы (как у set)

- lower_bound(key) — итератор на первый элемент не меньше key.
- upper_bound(key) — итератор на первый элемент больше key.
- equal_range(key) — возвращает пару (lower_bound, upper_bound).

## std::multimap — словарь с дубликатами ключей

Отличия от std::map

|Характеристика|	std::map<Key, T>|	std::multimap<Key, T>|
|-|-|-|
|Уникальность ключей|	Да|	Нет (допускаются дубликаты)|
|Оператор []|	Есть (создаёт элемент, если нет)|	Нет (неоднозначность)|
|insert возвращает|	pair<iterator, bool>|	iterator (всегда успешен)|
|erase(key)|	Удаляет 0 или 1 элемент|	Удаляет все элементы с данным ключом|
|count(key)|	0 или 1|	Количество пар с таким ключом|
|find(key)|	Итератор на единственную пару|	Итератор на первую из дубликатов |


### Основные методы и примеры
```
#include <map>
#include <string>
#include <iostream>

int main() {
    std::multimap<std::string, int> mm;
    
    // Вставка
    mm.insert({"Alice", 90});
    mm.insert({"Alice", 95});
    mm.insert({"Bob", 85});
    mm.insert({"Alice", 88});

    // Вывод всех пар
    for (const auto& [name, score] : mm) {
        std::cout << name << " -> " << score << std::endl;
    }
    // Порядок: Alice 90, Alice 95, Alice 88, Bob 85
    // (дубликаты ключей идут подряд, но порядок вставки среди дубликатов не гарантирован)

    // Подсчёт
    std::cout << "Alice's scores: " << mm.count("Alice") << std::endl; // 3

    // Поиск первого вхождения
    auto it = mm.find("Alice");
    if (it != mm.end()) {
        std::cout << "First Alice: " << it->second << std::endl; // может быть 90, 95 или 88
    }

    // Диапазон всех Alice
    auto range = mm.equal_range("Alice");
    for (auto it = range.first; it != range.second; ++it) {
        std::cout << it->second << " "; // выведет все оценки Alice
    }
    std::cout << std::endl;

    // Удаление одного элемента (по итератору)
    auto to_erase = mm.find("Bob");
    if (to_erase != mm.end()) mm.erase(to_erase);

    // Удаление всех Alice
    mm.erase("Alice"); // удаляет все три пары

    // После удаления остаётся пустой multimap
}
```

### Дополнительно 

```
iterator begin();
const_iterator begin() const;
Возвращает итератор для первого элемента в мультиотображении.
```
```
void clear();
Удаляет все элементы из мультиотображения.
```
````
size_type count(const key_type &k) const;
Возвращает число вхождений ключа k в мультиотображении (1 или 0).
```
```
bool empty() const;
Возвращает значение true, если данное мультиотображение пустое, и false в противном случае.
```
```
const_iterator end() const;
iterator end();
Возвращает итератор, указывающий на конец мультиотображения.
```
```
pair<iterator, iterator> equal_range(const key_type &k);
pair<const_iterator, const_iterator> equal_range(const key_type &k) const;
Возвращает пару итераторов, которые указывают на первый и последний элементы в мультиотображении,
содержащие заданный ключ.
```
```
void erase(iterator i);
Удаляет элемент, адресуемый итератором i.
```
```
void erase(iterator start, iterator end);
Удаляет элементы в диапазоне, задаваемом параметрами start и end.
```
```
size_type erase(const key_type &k);
Удаляет из мультиотображения элементы, ключи которых имеют значение k.
```
```
iterator find(const key_type &k);
const_iterator find(const key_type &k) const;
Возвращает итератор для заданного ключа. Если ключ не обнаружен, возвращает итератор до конца мультиотображения.
```
```
allocator_type get_allocator() const;
Возвращает распределитель мультиотображения.
```
```
iterator insert(iterator i, const value_type &val);
Вставляет значение val после элемента, заданным итератором i, возвращает итератор для этого элемента.
```
```
template <class InIter> void insert(InIter start,  InIter end);
Вставляет элементы заданного диапазона.
```
```
pair<iterator, bool> insert(const value_type &val);
Вставляет значение val в используемое мультиотображение.
Возвращает итератор для данного мультиотображения.
Элемент вставляет только в том случае, если его еще нет в мультиотображении. Если элемент был вставлен
возвращает пару pair<iterator, true>, в противном случае
```
```
pair<iterator, false>
key_compare key_comp() const;
Возвращает объект-функцию, которая сравнивает ключи.
```
```
iterator lower_bound(const key_type &k);
const_iterator lower_bound(const key_type &k) const;
Возвращает итератор для первого элемента в мультиотображении, ключ которого равен значению k или больше
этого значения.
```
```
size_type max_size() const;
Возвращает максимальное число элементов, которое может содержать мультиотображение.
```
```
reverse_iterator rbegin();
const_reverse_iterator rbegin() const;
Возвращает реверсивный итератор для конца мультиотображения.
```
```
reverse_iterator rbegin();
const_reverse_iterator rbegin() const;
Возвращает реверсивный итератор для начала мультиотображения.
```
```
size_type size() const;
Возвращает текущее количество элементов в мультиотображении.
```
```
void swap(multimap<Key, T, Comp, Allocator> &ob);
Выполняет обмен элементами данного мультиотображения и мультиотображения ob.
```
```
iterator upper_bound(const key_type &k);
const_iterator upper_bound(const key_type &k) const;
Возвращает итератор для первого элемента в мультиотображении, ключ которого больше заданного значения k.
```
```
value_compare value_comp() const;
Возвращает объект-функцию, которая сравнивает значения.
```

## Сложность операций (общая для multiset и multimap)

|Операция|	Сложность
|Поиск (find)|	O(log n)|
|Вставка|	O(log n)|
|Удаление по итератору|	 O(1)|
|Удаление по ключу (erase(key))	|O(log n + count)|
|count(key)	|O(log n)|
|lower_bound/upper_bound	|O(log n)|
|equal_range	|O(log n)|

## Контейнеры-адаптеры

- std::stack — реализует структуру данных стек (LIFO — Last In, First Out).
- std::queue — реализует структуру данных очередь (FIFO — First In, First Out).
- std::priority_queue — реализует очередь с приоритетом, где элемент с наивысшим приоритетом всегда находится в начале.

### std::stack — Классический стек

Стек — это структура данных, работающая по принципу «последним пришёл — первым вышел» (LIFO). \
Представьте себе стопку тарелок: вы всегда берёте верхнюю, и новую кладёте тоже наверх.

#### Интерфейс и методы

- push(const T& value) / emplace(Args&&... args): Добавляет элемент на вершину стека.
- pop(): Удаляет элемент с вершины стека. Ничего не возвращает.
- top(): Возвращает ссылку на элемент на вершине стека, не удаляя его.
- empty(): Проверяет, пуст ли
- стек.size(): Возвращает количество элементов в стеке.


## Служебное, только для меня )))


```
// array - массив обертка для статического массива
// 
void printAr(const array<int,30> li) {
    for (auto l : li)
        cout << l << " ";
    cout << endl;
}

int main8() {
    setlocale(LC_ALL, "rus");
    const int size = 30;
    array<int, size> arr = {1,2,3,4,5,6,7,8,9};   
    printAr(arr);

    arr.front();
    arr.back();

    arr.fill(77);
    printAr(arr);
    return 0;
}

int main() {
    setlocale(LC_ALL, "rus");

    map<string, int>mp = {{"str1",12},{"str2",33}};

    map<string, int>::iterator it  = mp.begin();
    
    cout << (*it).first << " "<< (*it).second << endl;

    printMp(mp);
    mp.insert({ "str3", 99 });
    mp.insert(pair<string,int>("str4", 199));
    mp.insert(make_pair("str5", 700));
    mp.emplace("str6", 1000);

    printMp(mp);

    mp.insert_or_assign("str3", 100);

    printMp(mp);

    it = mp.find("str4");
    if (it != mp.end())
        cout << (*it).first << " " << (*it).second << endl;


    cout << mp["str4"] << endl;
    cout << mp["str777"] << endl;


    mp["str4"] = 98766;
    cout << mp["str4"] << endl;

    mp["str777"] = 99999999;
    cout << mp["str777"] << endl;


    // at - проговорить

    return 0;
}


111111
int main() {
    std::vector<int> arr = {5, 2, 8, 2, 5, 1, 9, 3};
    
    // Помещаем все элементы в set (уникальные и отсортированные)
    std::set<int> unique_elements(arr.begin(), arr.end());
    
    // Выводим уникальные элементы в порядке возрастания
    for (int x : unique_elements) {
        std::cout << x << " ";
    }
    std::cout << std::endl;
    
    // Выводим их количество
    std::cout << "Количество уникальных элементов: " << unique_elements.size() << std::endl;
    
    return 0;
}

2222222
#include <iostream>
#include <set>
#include <algorithm>
#include <iterator>

// Объединение: A ∪ B
std::set<int> set_union(const std::set<int>& a, const std::set<int>& b) {
    std::set<int> result;
    std::set_union(a.begin(), a.end(),
                   b.begin(), b.end(),
                   std::inserter(result, result.begin()));
    return result;
}

// Пересечение: A ∩ B
std::set<int> set_intersection(const std::set<int>& a, const std::set<int>& b) {
    std::set<int> result;
    std::set_intersection(a.begin(), a.end(),
                          b.begin(), b.end(),
                          std::inserter(result, result.begin()));
    return result;
}

// Разность: A \ B (элементы, которые есть в A, но нет в B)
std::set<int> set_difference(const std::set<int>& a, const std::set<int>& b) {
    std::set<int> result;
    std::set_difference(a.begin(), a.end(),
                        b.begin(), b.end(),
                        std::inserter(result, result.begin()));
    return result;
}

// Пример использования
int main() {
    std::set<int> A = {1, 2, 3, 4, 5};
    std::set<int> B = {3, 4, 5, 6, 7};

    auto u = set_union(A, B);          // {1,2,3,4,5,6,7}
    auto i = set_intersection(A, B);   // {3,4,5}
    auto d = set_difference(A, B);     // {1,2}

    std::cout << "Union: ";
    for (int x : u) std::cout << x << " ";
    std::cout << "\nIntersection: ";
    for (int x : i) std::cout << x << " ";
    std::cout << "\nDifference (A\\B): ";
    for (int x : d) std::cout << x << " ";
}
```

```
Подсчёт гласных букв в строке
Условие:
Дана строка из латинских букв (строчные). Посчитайте, сколько раз встречается каждая гласная буква (a, e, i, o, u).
Выведите только те гласные, которые встречались, в алфавитном порядке, и их количество.

Пример:
"hello world" →
e : 1
o : 2
#include <iostream>
#include <map>
#include <string>

int main() {
    std::string text = "hello world";
    std::map<char, int> vowel_count;
    
    // Множество гласных для быстрой проверки
    std::set<char> vowels = {'a', 'e', 'i', 'o', 'u'};
    
    for (char ch : text) {
        if (vowels.count(ch)) {
            vowel_count[ch]++;
        }
    }
    
    for (const auto& [vowel, cnt] : vowel_count) {
        std::cout << vowel << " : " << cnt << std::endl;
    }
}
```

```
Частота символов в строке
Условие:
Дана строка. Подсчитайте частоту каждого символа (включая пробелы? пусть только буквы и цифры). Выведите символы в порядке возрастания (по коду ASCII) и их частоту.

Пример:
"abracadabra" →
a : 5
b : 2
c : 1
d : 1
r : 2


#include <iostream>
#include <map>
#include <string>

int main() {
    std::string text = "abracadabra";
    std::map<char, int> freq;
    
    for (char ch : text) {
        freq[ch]++;
    }
    
    for (const auto& [ch, count] : freq) {
        std::cout << ch << " : " << count << std::endl;
    }
}

```

```
Проверка изограммы
Условие:
Слово называется изограммой, если в нём нет повторяющихся букв (регистр не учитываем). Напишите программу, которая определяет, является ли введённое слово изограммой. Используйте set.

Пример:
"python" → "Да"
"hello" → "Нет" (буква 'l' повторяется)

#include <iostream>
#include <set>
#include <string>
#include <cctype> // for tolower

int main() {
    std::string word = "hello";
    std::set<char> letters;
    bool is_isogram = true;
    
    for (char ch : word) {
        char lower = std::tolower(ch);
        if (letters.count(lower)) {
            is_isogram = false;
            break;
        }
        letters.insert(lower);
    }
    
    std::cout << (is_isogram ? "Да" : "Нет") << std::endl;
}
```
